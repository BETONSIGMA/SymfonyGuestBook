{% extends 'base.html.twig' %}

{% block title %}HELL YEAH{% endblock %}

{% block body %}
    <h2>Give your feedback</h2>

    {% for conference in conferences %}
        <h4>{{ conference }}</h4>
        <p>
            <a href="{{ path('conference', {id: conference.id }) }}">Посмотреть</a>
        </p>
    {% endfor %}

{% endblock %}
